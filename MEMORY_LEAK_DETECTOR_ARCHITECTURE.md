# Memory Leak Detector - Architecture & Integration

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐         ┌──────────────────┐             │
│  │   Settings.jsx   │────────▶│  UserSettings    │             │
│  │                  │         │  State Management│             │
│  │ ┌──────────────┐ │         └──────────────────┘             │
│  │ │ Memory Leak  │ │                                           │
│  │ │   Toggle     │ │         ┌──────────────────┐             │
│  │ │  Checkbox    │ │────────▶│   API Request    │             │
│  │ └──────────────┘ │         │  POST /settings  │             │
│  └──────────────────┘         └──────────────────┘             │
│                                         │                        │
└─────────────────────────────────────────┼────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Backend (Spring Boot)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              SettingsController                           │  │
│  │  - Receives user settings                                │  │
│  │  - Saves to database                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              UserSettings Model                           │  │
│  │  - memoryLeakEnabled: Boolean                            │  │
│  │  - Persisted to database                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           CodeAnalysisEngine                              │  │
│  │  - Loads user settings                                   │  │
│  │  - Initializes all detectors                             │  │
│  │  - Configures detector enable/disable                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           Detector Collection                             │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ LongMethodDetector                                 │  │  │
│  │  │ MagicNumberDetector                                │  │  │
│  │  │ EmptyCatchDetector                                 │  │  │
│  │  │ ...                                                │  │  │
│  │  │ MemoryLeakDetector  ◄─── NEW DETECTOR             │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           MemoryLeakDetector                              │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ detect(CompilationUnit cu)                        │  │  │
│  │  │   │                                                │  │  │
│  │  │   ├─▶ checkUnclosedResources()                    │  │  │
│  │  │   │    - Finds FileInputStream, Connection, etc.  │  │  │
│  │  │   │    - Checks for .close() calls                │  │  │
│  │  │   │    - Verifies try-with-resources              │  │  │
│  │  │   │                                                │  │  │
│  │  │   ├─▶ checkStaticCollections()                    │  │  │
│  │  │   │    - Identifies static Map, List, Set         │  │  │
│  │  │   │    - Checks for clear/remove calls            │  │  │
│  │  │   │                                                │  │  │
│  │  │   ├─▶ checkListenerLeaks()                        │  │  │
│  │  │   │    - Finds addEventListener calls              │  │  │
│  │  │   │    - Looks for removeEventListener            │  │  │
│  │  │   │                                                │  │  │
│  │  │   └─▶ checkThreadLeaks()                          │  │  │
│  │  │        - Detects Thread/Executor creation         │  │  │
│  │  │        - Checks for shutdown mechanisms           │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Analysis Results                             │  │
│  │  - List of detected memory leak issues                   │  │
│  │  - Severity levels (🔴 🟡 🟠)                            │  │
│  │  - Suggestions for fixes                                 │  │
│  │  - Detailed explanations                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Database (MySQL)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  user_settings table                                             │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ id                                                     │     │
│  │ user_id                                                │     │
│  │ long_method_enabled                                    │     │
│  │ ...                                                    │     │
│  │ memory_leak_enabled  ◄─── NEW COLUMN (BOOLEAN)        │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. User Configuration Flow
```
User toggles Memory Leak Detection in Settings
    ↓
Frontend sends POST request to /settings/{userId}
    ↓
Backend saves memoryLeakEnabled to database
    ↓
Settings persisted for future analyses
```

### 2. Analysis Flow
```
User uploads Java project
    ↓
Backend loads user settings from database
    ↓
CodeAnalysisEngine initializes detectors
    ↓
If memoryLeakEnabled == true:
    ↓
    MemoryLeakDetector.detect() is called for each file
    ↓
    Detector analyzes AST for memory leak patterns
    ↓
    Issues are collected and formatted
    ↓
Results returned to frontend
    ↓
User views memory leak issues in report
```

## Detection Logic Flow

```
┌─────────────────────────────────────────────────────────────┐
│              MemoryLeakDetector.detect()                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│         Parse Java file into CompilationUnit (AST)           │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              Visit each MethodDeclaration                    │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Check      │  │   Check      │  │   Check      │
│  Unclosed    │  │   Static     │  │  Listener    │
│  Resources   │  │ Collections  │  │   Leaks      │
└──────────────┘  └──────────────┘  └──────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                            ▼
                  ┌──────────────┐
                  │   Check      │
                  │   Thread     │
                  │   Leaks      │
                  └──────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              Collect all detected issues                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│         Format issues with severity and suggestions          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│              Return List<String> of issues                   │
└─────────────────────────────────────────────────────────────┘
```

## Component Interactions

### Settings Component
```javascript
// Settings.jsx
const [settings, setSettings] = useState({
  // ... other settings
  memoryLeakEnabled: true  // NEW
});

// Checkbox in UI
<input 
  type="checkbox" 
  id="memoryLeakEnabled" 
  checked={settings.memoryLeakEnabled}
  onChange={(e) => handleInputChange('memoryLeakEnabled', e.target.checked)}
/>

// Save to backend
const handleSave = async () => {
  await api.post(`/settings/${userId}`, settings);
};
```

### Backend Integration
```java
// CodeAnalysisEngine.java
private void initializeDetectors() {
    // ... other detectors
    detectors.put("MemoryLeakDetector", new MemoryLeakDetector());
}

public void configureFromSettings(UserSettings settings) {
    // ... other configurations
    enabledDetectors.put("MemoryLeakDetector", 
        settings.getMemoryLeakEnabled() != null ? 
        settings.getMemoryLeakEnabled() : true);
}
```

## Issue Format

```
Severity: 🔴 (Critical) / 🟡 (High) / 🟠 (Medium)
Type: [MemoryLeak]
Location: FileName.java:LineNumber
Description: Resource 'variableName' in method 'methodName' may not be closed
Suggestions: Use try-with-resources or ensure close() is called in finally block
DetailedReason: Unclosed resources like streams, connections, or readers can cause 
                memory leaks as they hold references and prevent garbage collection
```

## Integration Points

1. **Frontend → Backend**
   - Settings API: `/settings/{userId}` (GET/POST)
   - Analysis API: `/upload` (POST)

2. **Backend → Database**
   - UserSettings entity persistence
   - JPA repository operations

3. **Backend → Detector**
   - CodeAnalysisEngine orchestration
   - Detector lifecycle management

4. **Detector → AST**
   - JavaParser library
   - AST traversal and analysis

## Performance Characteristics

- **Memory Usage**: Minimal (< 10MB per analysis)
- **CPU Usage**: Low (parallel with other detectors)
- **Analysis Time**: ~50-100ms per file
- **Scalability**: Linear with file count

## Error Handling

```
User Settings Load Failure
    ↓
Use default settings (memoryLeakEnabled = true)
    ↓
Continue analysis

Detector Exception
    ↓
Log error and continue with other detectors
    ↓
Report detector error in results

Parse Error
    ↓
Skip file and report parse error
    ↓
Continue with remaining files
```

## Summary

The Memory Leak Detector is seamlessly integrated into DevSync's existing architecture:
- ✅ Follows existing detector pattern
- ✅ Configurable via Settings UI
- ✅ Persisted in database
- ✅ Runs automatically during analysis
- ✅ Minimal performance impact
- ✅ Comprehensive error handling
